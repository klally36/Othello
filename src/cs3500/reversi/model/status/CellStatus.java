package cs3500.reversi.model.status;


/**
 * Represents the status of a cell on the board. EMPTY: the cell is empty BLACK: the cell holds a
 * black piece WHITE: the cell holds a white piece
 */
public enum CellStatus {
  EMPTY, BLACK, WHITE;
}
